/*
    # Complex macro
*/

/* takes two strings and output them */
macro my_output!(x, y) {
    output(x);
    output(y);
}

macro twice!(x) {
    x;
    x;
}

/* using macro */
twice!(my_output!("1", "2"));
